# Screenshots

Drop PNGs here to populate the README "Screenshots" section.
Recommended captures:

- `dashboard.png` — Streamlit app at http://localhost:8501 showing demand + hourly profile
- `mlflow_experiments.png` — MLflow UI runs list for `energy-forecasting` experiment
- `mlflow_shap_beeswarm.png` — SHAP summary plot logged as artifact
- `mlflow_registry.png` — `energy-demand-forecaster@staging` v2 in the Models tab
- `swagger.png` — FastAPI /docs page with /predict and /anomaly endpoints

Keep PNGs under 500 KB each — compress with `pngquant` or similar before committing.
